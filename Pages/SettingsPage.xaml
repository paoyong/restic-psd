<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="acorn.Pages.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="Transparent">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="36,28,36,36" Spacing="20" MaxWidth="820">
            <TextBlock Text="Settings" Style="{StaticResource TitleTextBlockStyle}" />

            <InfoBar IsOpen="True"
                     IsClosable="False"
                     Severity="Informational"
                     Title="General app settings"
                     Message="This page is reserved for app-wide preferences, behavior, and future Acorn settings." />

            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    CornerRadius="8"
                    Padding="24"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1"
                    Opacity="0.6">
                <StackPanel Spacing="12">
                    <TextBlock Text="Application preferences" Style="{StaticResource SubtitleTextBlockStyle}" />
                    <TextBlock Text="Placeholder content for startup behavior, notifications, appearance, and other global preferences."
                               Style="{StaticResource BodyTextBlockStyle}"
                               TextWrapping="Wrap" />
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</Page>
