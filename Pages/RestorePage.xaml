<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="acorn.Pages.RestorePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="Transparent">

    <StackPanel Margin="36,28,36,36" Spacing="20" MaxWidth="800">

        <TextBlock Text="Restore" Style="{StaticResource TitleTextBlockStyle}" />

        <InfoBar IsOpen="True"
                 IsClosable="False"
                 Severity="Informational"
                 Title="Coming soon"
                 Message="The Restore panel will let you browse snapshots by date, preview files, and restore individual files or entire folders from any backup." />

        <!-- Placeholder layout -->
        <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                CornerRadius="8" Padding="24" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1"
                Opacity="0.4">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="200" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <StackPanel Spacing="8">
                    <TextBlock Text="Snapshots" Style="{StaticResource SubtitleTextBlockStyle}" />
                    <Border Height="200" Background="{ThemeResource LayerFillColorDefaultBrush}" CornerRadius="4" />
                </StackPanel>
                <StackPanel Grid.Column="1" Margin="16,0,0,0" Spacing="8">
                    <TextBlock Text="Files" Style="{StaticResource SubtitleTextBlockStyle}" />
                    <Border Height="200" Background="{ThemeResource LayerFillColorDefaultBrush}" CornerRadius="4" />
                </StackPanel>
            </Grid>
        </Border>

    </StackPanel>
</Page>
