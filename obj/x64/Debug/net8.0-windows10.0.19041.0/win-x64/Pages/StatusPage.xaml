<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="acorn.Pages.StatusPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="Transparent">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="36,28,36,36" Spacing="20" MaxWidth="820">
            <TextBlock Text="Status" Style="{StaticResource TitleTextBlockStyle}" />

            <InfoBar IsOpen="True"
                     IsClosable="False"
                     Severity="Informational"
                     Title="Coming soon"
                     Message="This status page will show recent backup runs, repository health, and the last activity for each configured schedule." />

            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    CornerRadius="8"
                    Padding="24"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1"
                    Opacity="0.6">
                <StackPanel Spacing="12">
                    <TextBlock Text="Recent activity" Style="{StaticResource SubtitleTextBlockStyle}" />
                    <TextBlock Text="Placeholder content for backup history, task execution results, and snapshot counts."
                               Style="{StaticResource BodyTextBlockStyle}"
                               TextWrapping="Wrap" />
                </StackPanel>
            </Border>

            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                    CornerRadius="8"
                    Padding="24"
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1"
                    Opacity="0.6">
                <StackPanel Spacing="12">
                    <TextBlock Text="Health checks" Style="{StaticResource SubtitleTextBlockStyle}" />
                    <TextBlock Text="Placeholder content for repository checks, warnings, and other status signals."
                               Style="{StaticResource BodyTextBlockStyle}"
                               TextWrapping="Wrap" />
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</Page>

