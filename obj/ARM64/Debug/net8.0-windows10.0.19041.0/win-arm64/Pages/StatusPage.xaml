<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="acorn.Pages.StatusPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="Transparent">

    <StackPanel Margin="36,28,36,36" Spacing="20" MaxWidth="800">

        <TextBlock Text="Status" Style="{StaticResource TitleTextBlockStyle}" />

        <InfoBar IsOpen="True"
                 IsClosable="False"
                 Severity="Informational"
                 Title="Coming soon"
                 Message="The Status panel will show live backup activity, last run times, and snapshot counts for each schedule." />

        <!-- Placeholder skeleton cards -->
        <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                CornerRadius="8" Padding="24" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1"
                Opacity="0.4">
            <StackPanel Spacing="12">
                <TextBlock Text="Every 30 minutes" Style="{StaticResource SubtitleTextBlockStyle}" />
                <ProgressBar IsIndeterminate="False" Value="0" />
                <TextBlock Text="Last run: —" Style="{StaticResource CaptionTextBlockStyle}" />
            </StackPanel>
        </Border>

        <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                CornerRadius="8" Padding="24" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1"
                Opacity="0.4">
            <StackPanel Spacing="12">
                <TextBlock Text="Daily" Style="{StaticResource SubtitleTextBlockStyle}" />
                <ProgressBar IsIndeterminate="False" Value="0" />
                <TextBlock Text="Last run: —" Style="{StaticResource CaptionTextBlockStyle}" />
            </StackPanel>
        </Border>

    </StackPanel>
</Page>

